name: non_sdk_deps
version: 0.0.0
description: What we need that the sdk doesn't provide
dependencies:
  tuple: '^1.0.1'
  angular_ast: '^0.5.6'
dev_dependencies:
  test_reflective_loader: '^0.1.8'
  test: '^1.2.0'
